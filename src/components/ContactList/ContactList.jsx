export const ContactList = data => {
  console.log(data);
  return (
    <div>
      <ul>
        {/* {data.map(dat => (
          <li key={dat.id}></li>
        ))} */}
      </ul>
    </div>
  );
};
